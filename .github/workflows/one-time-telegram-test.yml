name: One-Time Telegram Test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  send-test-message:
    if: github.event_name == 'workflow_dispatch' || contains(github.event.head_commit.message, 'Send one-time telegram no-results test')
    runs-on: ubuntu-latest

    steps:
      - name: Send one-time Telegram test message
        id: telegram-test
        continue-on-error: true
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python - <<'PY' > telegram-test.log 2>&1
          import json
          import os
          import sys
          import urllib.request
          import urllib.error

          message = (
              "Тестовое сообщение: проверка выполнена, новых подходящих билетов "
              "сейчас не найдено. Это разовая проверка; дальше бот будет писать "
              "только когда найдёт новые варианты."
          )

          token = os.environ.get("TELEGRAM_BOT_TOKEN", "").strip()
          chat_id = os.environ.get("TELEGRAM_CHAT_ID", "").strip()
          if not token:
              raise RuntimeError("TELEGRAM_BOT_TOKEN is empty or missing")
          if not chat_id:
              raise RuntimeError("TELEGRAM_CHAT_ID is empty or missing")

          payload = json.dumps(
              {
                  "chat_id": chat_id,
                  "text": message,
                  "disable_web_page_preview": False,
              }
          ).encode("utf-8")

          request = urllib.request.Request(
              f"https://api.telegram.org/bot{os.environ['TELEGRAM_BOT_TOKEN']}/sendMessage",
              data=payload,
              headers={"Content-Type": "application/json"},
              method="POST",
          )

          try:
              with urllib.request.urlopen(request, timeout=30) as response:
                  body = response.read().decode("utf-8", errors="replace")
                  print(body)
                  status_code = response.getcode()
                  if status_code < 200 or status_code >= 300:
                      raise RuntimeError(
                          f"Telegram request failed with status {status_code}: {body}"
                      )
          except urllib.error.HTTPError as exc:
              body = exc.read().decode("utf-8", errors="replace")
              print(body, file=sys.stderr)
              raise RuntimeError(f"Telegram HTTP error {exc.code}: {body}") from exc
          PY

      - name: Show Telegram test log
        if: always()
        run: cat telegram-test.log

      - name: Write Telegram test summary
        if: always()
        run: |
          echo "## Telegram test diagnostic summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Step outcome: ${{ steps.telegram-test.outcome }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Log" >> $GITHUB_STEP_SUMMARY
          echo '```text' >> $GITHUB_STEP_SUMMARY
          cat telegram-test.log >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Fail workflow if Telegram test failed
        if: steps.telegram-test.outcome != 'success'
        run: exit 1
